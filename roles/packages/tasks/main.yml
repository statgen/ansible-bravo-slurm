---
- name: Install dependencies for pipeline tooling
  ansible.builtin.apt:
    pkg:
      - jq
      - openjdk-11-jre-headless
      - git
      - cmake
      - zlib1g-dev
      - libbz2-dev
      - liblzma-dev
      - perl
      - cpanminus
      - openssl
      - libminizip1
      - minizip
      - unzip
    state: present
    update_cache: yes
